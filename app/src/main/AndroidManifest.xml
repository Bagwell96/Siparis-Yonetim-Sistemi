<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mertkavrayici.ordertrackingsystem">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    
    <application
        android:name=".StarterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
       
        <activity android:name=".CustomerOrder" />
        <activity android:name=".Chat" />
        <activity android:name=".OrderProductPrice" />
        <activity android:name=".ReportsDetails" />
        <activity android:name=".Reports" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OrdersDetails" />
        <activity android:name=".AddOrder3" />
        <activity android:name=".ProductsDetails" />
        <activity android:name=".CustomersDetails" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AddCustomers2"
            android:label="@string/title_activity_add_customers2" />
        <activity android:name=".AddOrder2" />
        <activity android:name=".AddOrders" />
        <activity android:name=".Orders" />
        <activity android:name=".AddProducts" />
        <activity android:name=".Products" />
        <activity android:name=".AddCustomers" />
        <activity android:name=".Customers" />
        <activity
            android:name=".Menu1"
            android:theme="@style/SplashTheme" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>